# Factory agent entrypoint (Cline)

This file exists solely to redirect Cline to the canonical Factory
agent entrypoint. It is not an independent doctrine source.

`AGENTS.md` is the canonical LLM/coding-agent entrypoint for this
repository.

Before implementation, review, testing, qualification, evidence work, or
executing repository-changing commands:

1. Read `AGENTS.md`.
2. Follow every canonical doctrine and authoritative command referenced
   there (for example `FACTORY_GIT_WORKTREE_POLICY` and the verifier
   scripts it names).
3. Do not treat this file as an independent doctrine source.
4. If this file and `AGENTS.md` appear to conflict, `AGENTS.md` and the
   canonical doctrine files it references are authoritative.

One source of truth. Do not duplicate doctrine here.
